# Backend environment variables
# Spoonacular API key used by the FastAPI service to call external APIs.
# The backend should read this value via environment variable (do not hardcode).
SPOONACULAR_API_KEY=

# Optional: server configuration
# HOST=0.0.0.0
# PORT=8000
